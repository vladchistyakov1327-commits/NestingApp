# Production Manager Pro - Extended Modules

Полнофункциональная система управления производством с учётом материалов, зарплаты и элементами геймификации.

**Версия:** 1.0.0  
**Статус:** 🔴 Разработка → 🔵 GitHub CI/CD (готово к сборке)

---

## ✅ Реализовано

### Модуль 1: Управление материалами ✅
- [x] **Material.h/.cpp** – класс для одного материала с методом `isBelowMinimum()`
- [x] **MaterialsForm.h/.cpp** – форма "Справочник материалов" (добавить/удалить/сохранить)
- [x] **Norm.h/.cpp** – модель норм расхода материалов на изделия
- [x] **Receipt.h/.cpp** – модель для учёта приходов материалов
- [x] **ReceiptForm.h/.cpp** – форма "Приход материалов" с кнопкой "Оприходовать"
- [x] **ShipmentForm.h/.cpp** – форма "Отгрузка" с **автоматическим списанием материалов**
- [x] **Movement.h/.cpp** – модель для журнала движения материалов
- [x] **MovementForm.h/.cpp** – форма "Движение материалов" (только чтение)
- [x] **MaterialDelegate.h/.cpp** – условное форматирование (красная подсветка при `current < minimum`)
- [x] **InventoryManager.h/.cpp**:
  - ✅ `addReceipt()` – добавить приход, обновить остаток, записать в журнал
  - ✅ `writeOffForShipment()` – автоматически списать материалы при отгрузке

### Модуль 2: Кадры и зарплата ✅
- [x] **EmployeesForm.h/.cpp** – форма "Штат" (ФИО, должность, дата рождения, план)
- [x] **SalaryForm.h/.cpp** – форма "Зарплата" (план, факт, разница, "выплачено")
- [x] **PerformedForm.h/.cpp** – форма "Выполнено" (операции с фактом и контролем качества)
- [x] Расширенная БД: таблицы `employees`, `salary`, `performed`, `achievements`

### Интеграция с БД ✅
- [x] **DatabaseManager.h/.cpp** – singleton для SQLite (`production.db`)
- [x] **createSchema()** – автоматическое создание всех таблиц при запуске
- [x] Использование `QSqlTableModel` для привязки данных к таблицам

### UI и главное окно ✅
- [x] **MainWindow.h/.cpp** – главное окно с `QTabWidget` (7 вкладок):
  1. ✅ Справочник материалов
  2. ✅ Приход материалов
  3. ✅ Отгрузка (с автолистанием)
  4. ✅ Движение материалов
  5. ✅ Штат
  6. ✅ Зарплата
  7. ✅ Выполнено

### CI/CD и GitHub ✅
- [x] **.github/workflows/build.yml** – автоматическая сборка на Windows/Linux/macOS через GitHub Actions
- [x] **CMakeLists.txt** – конфигурация сборки для Qt 6
- [x] **.gitignore** – исключения для git и сборки

### Документация ✅
- [x] **README.md** (этот файл) – краткий обзор
- [x] **GITHUB_README.md** – полная документация для GitHub
- [x] **GITHUB_SETUP.md** – инструкция по настройке GitHub Actions
- [x] **TESTING.md** – пошаговый сценарий тестирования

---

## 📁 Структура проекта
```
ZP.xlsx               # example table provided by user
CMakeLists.txt        # cmake build configuration
src/                  # C++ source files
  main.cpp            # entry point
  MainWindow.h/cpp    # main application window (tabs)
  
  # Data models
  Material.h/cpp
  Norm.h/cpp
  Receipt.h/cpp
  Movement.h/cpp
  
  # Database & inventory
  DatabaseManager.h/cpp    # singleton, manages QSqlDatabase
  InventoryManager.h/cpp   # methods for receipts & write-offs
  
  # UI Forms
  MaterialsForm.h/cpp      # materials table + low-stock highlighting
  ReceiptForm.h/cpp        # incoming materials
  ShipmentForm.h/cpp       # shipments + automatic write-off trigger
  MovementForm.h/cpp       # stock journal (read-only)
  
  # UI Helpers
  MaterialDelegate.h/cpp   # conditional formatting (red rows for low stock)
  
  # Future: employee rating, notifications, etc.
  NotificationSystem.h/cpp
  EmployeeRating.h/cpp
README.md
```

## 💡 Next Steps & Suggestions

1. **Database integration** ✅ – connect classes to a SQLite/QSql database; add DAO layers or use `QSqlTableModel`.
   - a convenient wrapper (`DatabaseManager`) opens a single `QSQLITE` database file and provides access.
   - `createSchema()` автоматически создаёт расширенную схему с таблицами:
   
   **Inventory** (склад и материалы):
   ```sql
   materials      -- справочник материалов (имя, единица, цена, текущий, минимум, поставщик)
   norms          -- нормы расхода (изделие → материал → количество)
   receipts       -- приходы материалов (дата, материал, кол-во, цена, поставщик)
   movements      -- журнал всех движений (дата, тип, материал, кол-во, основание)
   ```
   
   **Products & Shipments** (изделия и отгрузки):
   ```sql
   products       -- справочник товаров (имя, описание, цена, себестоимость)
   shipments      -- отгрузки (дата, изделие, кол-во, заказчик, статус)
   ```
   
   **Employees & Payroll** (штат и зарплата):
   ```sql
   employees      -- сотрудники (имя, должность, дата рождения, дата найма, план, тип зарплаты)
   salary         -- зарплата (сотр, месяц, план, факт, выплачено?, разница)
   performed      -- выполненные работы (дата, сотр, операция, факт, контроль)
   achievements   -- бейджи (сотр, название бейджа, дата получения)
   ```
   
   **Notifications** (уведомления):
   ```sql
   notifications  -- журнал (тип, сообщение, приоритет, создано, прочитано)
   ```
2. **Автоматическое списание** – реализовано в `InventoryManager::writeOffForShipment`:
   - при добавлении записи отгрузки метод получает `product` и `productQty`.
   - запрашивает нормы расхода из таблицы `norms` для указанного изделия.
   - для каждого материала вычисляет списание (`норма * количество`) и уменьшает колонку `current` в `materials`.
   - в журнал `movements` записывается операция списания с указанием номера заказа/отгрузки (параметр `orderId`).
   - вызов этого метода нужно сделать из логики сохранения формы "Отгрузка".
3. **User interface** ✅ – Qt widget forms for each section:
   - `MaterialsForm` – таблица материалов с buttons для добавления/удаления; delegate для подсветки критичных остатков.
   - `ReceiptForm` – таблица приходов материалов; кнопка "Оприходовать" обновляет остатки.
   - `ShipmentForm` – таблица отгрузок; **кнопка "Отгрузить" вызывает `InventoryManager::writeOffForShipment`** – автоматическое списание материалов.
   - `MovementForm` – журнал движения материалов (только для чтения); записи создаются автоматически.
   - `MainWindow` – главное окно с `QTabWidget`, объединяющее все формы.
   - `MaterialDelegate` – стилизация таблицы: строка подсвечивается красным, если `current < minimum`.
   
4. **Cost calculations** – extend product catalog with computed columns (material cost, production cost, margin).
5. **Shipment handling** ✅ – `ShipmentForm::shipNow()` автоматически вызывает списание материалов; логика записана в `InventoryManager`.
6. **Settings dialog** – a model to persist notification preferences and thresholds.
7. **Badge logic** – evaluate rules after each month and store earned achievements, then display in employee profile.

### ✨ Improvement ideas

- **Batch operations** – allow importing materials and norms from CSV/Excel.
- **Alerts history** – archive old notifications for auditing.
- **Telegram/email integration** – use Qt network classes for automated weekly reports.
- **Customization** – make units, notification texts and colors configurable by the admin.

---

#### 🧾 Примеры кода
```cpp
// в main.cpp при старте
if (!DatabaseManager::instance().open("production.db")) {
    qFatal("Cannot open database");
}
// возможно выполнить миграции/создать таблицы, если не существуют
```

```cpp
// создание прихода материалов
InventoryManager::addReceipt(QDate::currentDate(), "Лист металлический 2мм", 100, 120.0, "Поставщик A");

// списание при отгрузке 5 единиц изделия "Ящик метал."
InventoryManager::writeOffForShipment("Ящик метал.", 5, "Order-123");
```

```cpp
// в модели Qt (например QSqlTableModel)
QSqlTableModel *matModel = new QSqlTableModel(parent, DatabaseManager::instance().database());
matModel->setTable("materials");
matModel->select();
view->setModel(matModel);
```

---

## 🛠️ Сборка и запуск

### Требования
- Qt 6.x (или Qt 5.15)
- CMake 3.16+
- Компилятор C++ с поддержкой C++17

### Компиляция
```bash
cd g:\My file\Programing\ZP
mkdir build
cd build
cmake ..
cmake --build .
```

### Запуск
```bash
./ProductionManager              # Linux/macOS
ProductionManager.exe            # Windows
```

---

## 🎯 Использование

**Сценарий: Приход материала → Отгрузка товара**

1. Откройте вкладку **"Справочник материалов"** → добавьте материалы с мин. остатками.
2. Откройте **"Приход материалов"** → добавьте поступления → нажмите "Оприходовать".
   - Остатки автоматически обновляются; красная подсветка исчезает если остаток ≥ минимума.
3. Откройте **"Отгрузка"** → добавьте заказ на товар → выберите строку → нажмите "Отгрузить (списать материалы)".
   - Система автоматически вычисляет нормы расхода материалов, уменьшает их остатки, записывает в журнал.
4. **"Движение материалов"** → просмотрите всю историю приходов/списаний с датами.

---

> 📎 Откройте `ZP.xlsx` – там можно посмотреть пример структуры таблиц и сверстать соответствующие SQL‑схемы.